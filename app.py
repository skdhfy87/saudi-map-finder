import streamlit as st
import random

# إعدادات الواجهة
st.set_page_config(page_title="كشاف السعودية", page_icon="🇸🇦")

st.title("📍 كشاف المواقع البعيدة (السعودية)")
st.info("هذه الأداة تعطيك إحداثيات عشوائية في المدينة التي تختارها بعيداً عن صخب المراكز التجارية.")

# قاعدة بيانات بسيطة لإحداثيات المدن الرئيسية لتسريع العمل
CITIES = {
    "جدة": (21.5433, 39.1728),
    "الهفوف": (25.3622, 49.5883),
    "الرياض": (24.7136, 46.6753),
    "الدمام": (26.4207, 50.0888),
    "المدينة المنورة": (24.4672, 39.6024),
    "أبها": (18.2164, 42.5053)
}

city_choice = st.selectbox("اختر المدينة:", list(CITIES.keys()))
count = st.number_input("عدد الأماكن:", min_value=1, max_value=10, value=5)

if st.button("توليد أماكن عشوائية"):
    base_lat, base_lon = CITIES[city_choice]
    
    st.subheader(f"نتائج مدينة {city_choice}:")
    
    for i in range(count):
        # توليد إحداثيات عشوائية في نطاق 10-15 كم
        # التغيير في الرقم 0.08 يزيد أو ينقص المسافة عن المركز
        r_lat = base_lat + random.uniform(-0.08, 0.08)
        r_lon = base_lon + random.uniform(-0.08, 0.08)
        
        # رابط جوجل ماب
        map_link = f"https://www.google.com/maps?q={r_lat},{r_lon}"
        
        with st.expander(f"📍 موقع مقترح رقم {i+1}"):
            st.write(f"الإحداثيات: `{r_lat:.5f}, {r_lon:.5f}`")
            st.link_button("افتح في خرائط جوجل", map_link)

st.divider()
st.caption("ملاحظة: هذه النقاط عشوائية تماماً، يرجى التأكد من طبيعة المنطقة عبر صور الأقمار الصناعية قبل الذهاب.")
