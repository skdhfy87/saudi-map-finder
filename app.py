import streamlit as st
import random

st.set_page_config(page_title="Sylon Premium", page_icon="📡")

st.markdown("<h1 style='text-align: center;'>📡 SYLON PREMIUM</h1>", unsafe_allow_html=True)
st.write("تم ضبط الرادار على أرقى الأحياء السكنية لضمان الخصوصية والهدوء.")

# قاعدة بيانات الأحياء الراقية (يمكنك إضافة المزيد لكل مدينة بنفس الطريقة)
NEIGHBORHOODS = {
    "مكة - الشوقية": (21.3740, 39.7920),
    "مكة - العوالي": (21.3480, 39.8970),
    "مكة - العزيزية": (21.3910, 39.8800),
    "الرياض - حطين": (24.7600, 46.6100),
    "الرياض - الياسمين": (24.8300, 46.6400),
    "جدة - أبحر الشمالية": (21.7600, 39.1200),
    "جدة - حي الشاطئ": (21.6000, 39.1100)
}

choice = st.selectbox("اختر الحي الراقي:", sorted(NEIGHBORHOODS.keys()))
count = st.slider("عدد النقاط المقترحة:", 1, 5, 3)

if st.button("بحث في النطاق السكني الفاخر"):
    base_lat, base_lon = NEIGHBORHOODS[choice]
    
    for i in range(count):
        # نطاق ضيق جداً (0.003) لضمان البقاء داخل أزقة الحي الهادئة
        r_lat = base_lat + random.uniform(-0.003, 0.003)
        r_lon = base_lon + random.uniform(-0.003, 0.003)
        
        # الرابط المطور لخرائط جوجل
        map_link = f"https://www.google.com/maps/search/?api=1&query={r_lat},{r_lon}"
        
        with st.container():
            st.markdown(f"**📍 موقع في {choice} - نقطة {i+1}**")
            st.link_button("فحص الموقع 🗺️", map_link)
            st.write("---")
